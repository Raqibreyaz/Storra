TODO:
  ISSUES:
    - what parts in the application is creating unnecessary complexity
    - handle these in error middleware:
      - razorpay specific errors
      - mongodb/mongoose specific errors
      - app specific errors
    - rate limitting and throttling correction
    - nodemailer failed to refresh token, need to publish app to production maybe
    - dangerous site error occuring on previewing a file
    - ErrorCode fixing in Error Responses
    - check for atomic Ops requirement wherever possible
    - make the subscription creation and user updation atomic
    - github logged in, but cookie didnt get saved, while google and local login works
  
  FEATURE_REQUEST:
    - import from google drive
    - toggling 2FA
  
  - deploy the frontend to netlify or any reliable place -> skip deploying to aws(s3 + cloudfornt) for now
  - cloudfront's filenaming and downloading problem -> remove cloudfront for now

LATER:
  - razorpay plan updation failing with, checking with a different card
      {
        statusCode: 400,
        error: {
          code: 'BAD_REQUEST_ERROR',
          description: "Can't update subscription immediately when card mandate is applicable"
        }
      }
  - Download as zip for both files and directories
  - sort by name,date,size etc.
  - handle case where user has paused subscription from too long
  - put user's files to glacier storage for low cost where needed
  - Move/copy files/directories from one directory to other
  - adding invoices feature in the dashboard
  - directory sharing functionality with the help of path property
  - filter
  - remove file extension from s3 object completely to maintain atomic updates

DONE:
    - otp input goes hidden on page refresh, login and signup page
    - adding landing page
    - light/dark mode feature