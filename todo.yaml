TODO:
  ISSUES:
    - using cron job:
      - cleanup zombie s3 objects
      - initiated file entries in DB
      - created subscriptions
    - creating separate pages instead of section in the admin dashboard
    - centralizing duplicate code(like formatSize(bytes)) across the codebase
    - need current global storage usage status to compare with hard-capped global storage
    - nodemailer failed to refresh token, need to publish app to production maybe
    - dangerous site error occuring on previewing a file
    - github logged in, but cookie didnt get saved, while google and local login works
  
  FEATURE_REQUEST:
    - import from google drive
    - toggling 2FA
    - any mechanism to get notified by s3 when user has successfully uploaded the file, so we dont need to wait for user to notify us of file upload completion
  
  - deploy the frontend to netlify or any reliable place -> skip deploying to aws(s3 + cloudfornt) for now
  - cloudfront's filenaming and downloading problem -> remove cloudfront for now

LATER:
  - razorpay plan updation failing with, checking with a different card
      {
        statusCode: 400,
        error: {
          code: 'BAD_REQUEST_ERROR',
          description: "Can't update subscription immediately when card mandate is applicable"
        }
      }
  - Download as zip for both files and directories
  - sort by name,date,size etc.
  - handle case where user has paused subscription from too long
  - put user's files to glacier storage for low cost where needed
  - Move/copy files/directories from one directory to other
  - adding invoices feature in the dashboard
  - directory sharing functionality with the help of path property
  - filter

DONE:
    - otp input goes hidden on page refresh, login and signup page
    - adding landing page
    - light/dark mode feature
    - check for atomic Ops requirement wherever possible
    - handle these in error middleware:
      - razorpay specific errors
      - mongodb/mongoose specific errors
      - app specific errors
    - ErrorCode fixing in Error Responses
    - remove file extension from s3 object completely to maintain atomic updates, have to handle extension removal also from whole app as it may not be necessary
    - adding zod validation to routes where not used
    - sanitizing input where not performed
    - how the rate limiting part in the entire app can be made scalable, currently not every route is rate limited, many are rate limited but i dont feel it good, limits are taken from env vars else default limits are used, what else things you find broken or can be made significantly better here in rate limiting
    - what do you find which can be made significantly better for throttling, problems in current implementation, and how to make it easy, readable and less code
    - limiting no of users in the app with settings